# دليل النشر على Netlify 🚀

## الخطوات السريعة للنشر

### 1. إنشاء حساب Netlify
- اذهب إلى [Netlify](https://www.netlify.com/)
- قم بالتسجيل باستخدام حساب GitHub الخاص بك

### 2. ربط المشروع بـ Netlify

#### الطريقة الأولى: عبر واجهة Netlify (الأسهل)

1. **من لوحة تحكم Netlify:**
   - اضغط على "Add new site" → "Import an existing project"
   - اختر "Deploy with GitHub"
   - قم بتصريح Netlify للوصول لحسابك على GitHub
   - اختر المستودع: `mohamed-khairy-5i/shop`

2. **إعدادات البناء:**
   ```
   Build command: npm run build
   Publish directory: public
   Functions directory: netlify/functions
   ```

3. **اضغط "Deploy site"**

#### الطريقة الثانية: عبر Netlify CLI (للمطورين)

```bash
# تثبيت Netlify CLI
npm install -g netlify-cli

# تسجيل الدخول
netlify login

# ربط المشروع
netlify init

# النشر
netlify deploy --prod
```

### 3. إعدادات مهمة بعد النشر

#### إعادة توجيه API
تم تكوين ملف `netlify.toml` تلقائياً لإعادة توجيه طلبات `/api/*` إلى Netlify Functions.

#### المتغيرات البيئية (إن وجدت)
إذا كان لديك أي متغيرات بيئية، أضفها في:
- Site settings → Environment variables

### 4. التحقق من النشر

بعد النشر الناجح، ستحصل على رابط مثل:
```
https://your-site-name.netlify.app
```

اختبر الروابط التالية:
- الصفحة الرئيسية: `https://your-site-name.netlify.app/`
- API المنيو: `https://your-site-name.netlify.app/api/menu`
- API الإضافات: `https://your-site-name.netlify.app/api/sides-drinks`

### 5. النشر التلقائي

الآن عند أي تحديث في GitHub (push to main)، سيتم النشر تلقائياً على Netlify! 🎉

## هيكل المشروع

```
webapp/
├── public/                  # الملفات الثابتة (HTML, CSS, JS, Images)
│   ├── index.html          # الصفحة الرئيسية
│   └── static/             # الملفات الثابتة
│       ├── app.js          # منطق Frontend
│       └── styles.css      # التنسيقات
├── netlify/
│   └── functions/          # Serverless Functions
│       ├── menu.js         # API المنيو
│       └── sides-drinks.js # API الإضافات
├── netlify.toml            # إعدادات Netlify
└── package.json            # التبعيات
```

## استكشاف الأخطاء

### المشكلة: "Page not found"
- **الحل**: تأكد من أن `netlify.toml` موجود وصحيح
- تحقق من أن `public` هو المجلد الصحيح

### المشكلة: API لا يعمل
- **الحل**: تحقق من أن `netlify/functions/` موجود
- تحقق من إعدادات التوجيه في `netlify.toml`

### المشكلة: الأنماط لا تظهر
- **الحل**: تأكد من مسارات الملفات في `index.html`
- تحقق من وجود الملفات في `public/static/`

## ملاحظات مهمة

✅ **المزايا المجانية في Netlify:**
- 100 GB نقل بيانات شهرياً
- Serverless Functions (125,000 طلب شهرياً)
- نشر تلقائي من GitHub
- شهادة SSL مجانية
- نطاق فرعي مجاني (.netlify.app)

⚠️ **القيود:**
- حد أقصى لحجم الـ Function: 50 MB
- وقت تنفيذ الـ Function: 10 ثوانٍ
- حجم الطلب: 6 MB

## الدعم

إذا واجهت أي مشكلة:
1. راجع [Netlify Docs](https://docs.netlify.com/)
2. تحقق من سجلات البناء في لوحة تحكم Netlify
3. تواصل مع فريق الدعم
